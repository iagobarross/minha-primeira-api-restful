spring:
  datasource:
    url: jdbc:postgresql://${PROXYHOST}:${PROXYPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true